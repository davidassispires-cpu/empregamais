const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];function irPara(p='home'){history.pushState({},'',p==='home'?location.pathname:location.pathname+'?pagina='+encodeURIComponent(p));abrirRota(p)}function mostrarPagina(id){$$('.pagina').forEach(x=>x.classList.remove('ativa'));const el=$('#pagina-'+id);if(el)el.classList.add('ativa');scrollTo(0,0)}function papelAtual(){return sessionStorage.getItem('empregaMaisPapel')||''}function abrirRota(p){if(p==='painel-empresa'&&papelAtual()!=='empresa')p='login-empresa';if(p==='painel-candidato'&&papelAtual()!=='candidato')p='login-candidato';mostrarPagina(p||'home')}function lerRota(){abrirRota(new URLSearchParams(location.search).get('pagina')||'home')}function loginDemo(tipo,e){e.preventDefault();sessionStorage.setItem('empregaMaisPapel',tipo);irPara(tipo==='empresa'?'painel-empresa':'painel-candidato')}function sair(){sessionStorage.removeItem('empregaMaisPapel');irPara('home')}addEventListener('popstate',lerRota);addEventListener('DOMContentLoaded',lerRota);